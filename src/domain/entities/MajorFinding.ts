/**
 * One notable observation from a persona's response, in the form used for
 * reporting: what happened, the evidence for it, and why it matters.
 */
export interface MajorFinding {
  observation: string;
  evidence: string;
  impact: string;
}
